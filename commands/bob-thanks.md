---
name: bob-thanks
description: Ask what came of your introductions, then draft the thank-yous into your Gmail drafts.
---

Run the `bob-thanks` skill.

Requires `intros.csv` in `${user_config.data_dir}`. If it isn't there, say so and
point at `/bob-scan` — do not ask questions about introductions Bob hasn't found.
